# data

Datasets go here. Not committed to git (see `.gitignore`) — download instructions
below so the repo stays light and reproducible.

## makemore (names.txt)

```bash
curl -O https://raw.githubusercontent.com/karpathy/makemore/master/names.txt
```

## GPT (tiny shakespeare, input.txt)

```bash
curl -O https://raw.githubusercontent.com/karpathy/char-rnn/master/data/tinyshakespeare/input.txt
```
